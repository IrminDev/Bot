<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> - Admin Panel</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            padding: 20px;
        }
        .container {
            background: white;
            border-radius: 10px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            padding: 40px;
            max-width: 600px;
            width: 100%;
            margin-top: 50px;
        }
        h2 { color: #2c3e50; margin-bottom: 30px; text-align: center; }
        .form-label { display: block; margin-bottom: 5px; font-weight: 600; color: #333; }
        .form-control, .form-select { width: 100%; padding: 10px; margin-bottom: 20px; border: 1px solid #ccc; border-radius: 5px; }
        .btn { display: inline-block; padding: 10px 20px; text-decoration: none; border-radius: 5px; font-weight: 600; border: none; }
        .btn-success { background-color: #2ecc71; color: white; }
        .btn-secondary { background-color: #95a5a6; color: white; }
        .btn:hover { opacity: 0.9; }
        .alert { padding: 15px; margin-bottom: 20px; border-radius: 4px; background-color: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
    </style>
</head>
<body>

    <% 
        // Lógica para determinar si es edición o creación
        const actionUrl = isEdit ? `/admin/users/${user.id}?_method=PUT` : '/admin/users';
        const formTitle = isEdit ? `Editar Usuario: ${user.username}` : 'Crear Nuevo Usuario';
    %>

    <div class="container">
        <a href="/admin/users" class="btn btn-secondary" style="float: right;">Volver</a>
        <h2><%= formTitle %></h2>
        <hr>

        <%# Mostrar errores de express-validator %>
        <% if (errors && errors.length > 0) { %>
            <div class="alert" role="alert">
                <ul>
                    <% errors.forEach(error => { %>
                        <li><%= error %></li>
                    <% }) %>
                </ul>
            </div>
        <% } %>

        <form action="<%= actionUrl %>" method="POST">
            <%# Token CSRF: Obligatorio para peticiones que modifican datos %>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">

            <div class="mb-3">
                <label for="username" class="form-label">Nombre de Usuario</label>
                <input type="text" class="form-control" id="username" name="username" value="<%= user.username || '' %>" required>
            </div>

            <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" name="email" value="<%= user.email || '' %>" required>
            </div>

            <div class="mb-3">
                <label for="password" class="form-label">Contraseña <%= isEdit ? '(dejar vacío para no cambiar)' : '*' %></label>
                <input type="password" class="form-control" id="password" name="password" <%= isEdit ? '' : 'required' %>>
                <% if (isEdit) { %>
                    <small style="color: #666;">Ingresa una nueva contraseña solo si deseas cambiarla.</small>
                <% } %>
            </div>

            <div class="mb-3">
                <label for="role" class="form-label">Rol</label>
                <select class="form-select" id="role" name="role" required>
                    <option value="user" <%= user.role === 'user' || !user.role ? 'selected' : '' %>>Usuario Regular</option>
                    <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>Administrador</option>
                </select>
            </div>

            <button type="submit" class="btn btn-success me-2">
                <%= isEdit ? 'Actualizar Usuario' : 'Crear Usuario' %>
            </button>
            <a href="/admin/users" class="btn btn-secondary">Cancelar</a>
        </form>
    </div>
</body>
</html>